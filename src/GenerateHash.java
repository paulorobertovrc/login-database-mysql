
public class GenerateHash {
    public static String generateHash(String password) {
        System.out.println("Gerando hash da senha " + password);
        String passwordHash = password;
        return passwordHash;
    }
}
